---
description: Show QR code to continue this session on your phone
allowed-tools: Bash(claude-phone-pair*)
---

Run the command `claude-phone-pair` and display its output verbatim. Do not analyze, summarize, or modify the output — the QR code drawing must be preserved exactly as printed. If the command fails because `CLAUDE_PHONE_RPC_URL` is not set, explain to the user that they need to run `claude-phone` instead of `claude` for this feature to work.
