# Rust
target/
**/*.rs.bk

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Editor
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.local
config.local.toml
# Local dev configs hold an api_key — keep them out of git on purpose.
# Use scripts/smoke-localhost.sh to regenerate when needed.
*-dev.toml

# Logs
*.log

# CT monitor cache (TM-TLS.4) — baseline of crt.sh serial numbers,
# stored in GitHub Actions cache, never committed.
.ct-baseline
.ct-new
.ct-new.raw
