import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
  return <div>Claude Phone — placeholder</div>;
}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
